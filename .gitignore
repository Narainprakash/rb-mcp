# Environment variables
.env
.env.*
!.env.example

# User config (contains account-specific settings)
config.yaml
!.env.example

# Database
hermes.db
hermes_mt.db
*.sqlite3

# Database snapshots (contain trade history and password hashes)
backups/

# Python Virtual Environment
venv/
env/
.venv/

# State files
.since_id
HALT
HALT_TRADING

# Robinhood OAuth tokens - grants trading access to the agentic account.
# One file per dashboard user (.robinhood_token.<user_id>.json), plus the
# legacy process-wide file from before credentials were per-user.
.robinhood_token*.json

# Python Cache
__pycache__/
*.py[cod]
*$py.class
.pytest_cache/

# OS Files
.DS_Store
Thumbs.db
